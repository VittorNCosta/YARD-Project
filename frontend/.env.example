# URL base da API do backend YardControl.
# Deve incluir o prefixo /api (montado em backend/src/infra/http/app.ts).
# Porta padrão definida em backend/.env (PORT=3000).
VITE_API_URL=http://localhost:3000/api
