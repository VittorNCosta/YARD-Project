services:
  mongo:
    image: mongo:7
    container_name: yard-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - yard-mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  yard-mongo-data:
